# LOCAL DEVELOPMENT — configure for your environment
# In production, these must be set as build-time environment variables in Vercel.
# See .env.local.example for required variables.
NEXT_PUBLIC_API_URL=http://localhost:4000/v1
NEXT_PUBLIC_WS_URL=http://localhost:4000
